@model Domain_GestaoHospitalar.Models.Paciente

@{
    ViewData["Title"] = "Adicionar Paciente";
}

<div class="row">
    <div class="panel panel-heading">
        <h3>@ViewData["Title"]</h3>
    </div>
    <div class="panel panel-body">
        <div class="col-md-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <partial name="_Edit_Create" />

                <div class="form-group">
                    <input type="submit" value=@ViewData["Title"] class="btn btn-info" />
                    <a asp-action="Index" class="btn btn-default">Voltar para Lista</a>
                </div>
            </form>
        </div>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
